<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <!-- 1.服务端应用名称：随便写（保证唯一:和项目名一致）  -->
    <dubbo:application name="export_company_service">
        <dubbo:parameter key="qos.enable" value="false"></dubbo:parameter>
    </dubbo:application>


    <!--2.配置注册中心  注册中心的地址-->
    <dubbo:registry address="zookeeper://192.168.233.99:2181"></dubbo:registry>

    <!--
        3.配置请求协议  此dubbo服务的请求端口（和tomcat端无关：不能一致）
            port（端口）：服务提供者的真实请求端口
     -->
    <dubbo:protocol name="dubbo" port="20882"></dubbo:protocol>

    <!--4.配置dubbo服务提供者的包扫描-->
    <dubbo:annotation package="cn.itcast.service"/>
</beans>